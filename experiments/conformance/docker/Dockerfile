# Conformance cross-ISA leg: x86-64 Python + numpy, pinned.
# Built with: docker build --platform linux/amd64 -t rnr-conformance-amd64 .
FROM python:3.12-slim
RUN pip install --no-cache-dir numpy==2.4.6
